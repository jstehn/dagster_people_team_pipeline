default:
  outputs:
    staff:
      dataset: staff
      job_execution_timeout_seconds: 300
      job_retries: 1
      keyfile_json: '{{ env_var(''GCP_CREDS'') }}'
      location: US
      method: service-account
      priority: interactive
      project: '{{ env_var(''GCP_PROJECT'') }}'
      threads: 4
      type: bigquery
  target: '{{ env_var("DBT_TARGET") }}'
